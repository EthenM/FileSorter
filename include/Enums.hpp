#pragma once

namespace FileSorterProgram {
    
    enum class SortType {
        Unknown,
        Year,
        Month
    };

    enum class TranserType {
        Unknown,
        Move,
        Copy
    };

}
